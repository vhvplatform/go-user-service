# Example environment variables (do not put secrets here)
# Copy this file to create your local .env files (for example: .env.local) and fill in the secret values.

VITE_ENVIRONMENT=local

# API endpoints
# For local development we recommend using the Vite dev server proxy at /api
# which forwards to your backend (e.g. http://localhost:8082). This avoids CORS issues.
VITE_API_URL_LOCAL=/api
VITE_API_URL_DEV=https://dev.api.example.com
VITE_API_URL_STAGING=https://staging.api.example.com
VITE_API_URL_PRODUCTION=https://api.example.com

VITE_API_BASE_URL=/api
VITE_API_TIMEOUT=10000

# Feature toggles
VITE_USE_MOCK_API=false
VITE_USE_MOCK_DATA=false
VITE_FEATURE_ANALYTICS=false
VITE_FEATURE_NOTIFICATIONS=false
VITE_FEATURE_REPORTS=false
VITE_FEATURE_TEAMS=false

# Service URLs (fill with actual service endpoints)
VITE_USER_SERVICE_URL=
VITE_AUTH_SERVICE_URL=
VITE_FILE_SERVICE_URL=
VITE_GO_USER_SERVICE_URL=

# Third-party integrations (keep keys out of repo)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# App metadata
VITE_APP_NAME="React User Service"
VITE_APP_VERSION=0.0.0
VITE_APP_ENV=local
VITE_TENANT_ID=
VITE_TENANT_NAME=

# Pagination / caching
VITE_DEFAULT_PAGE_SIZE=20
VITE_MAX_PAGE_SIZE=100
VITE_CACHE_TTL=3600
VITE_ENABLE_CACHE=false

# Debug / logging
VITE_DEBUG_MODE=false
VITE_LOG_LEVEL=info
VITE_ENABLE_DEBUG=false

# Security / request settings
VITE_FORCE_HTTPS=false
VITE_SESSION_TIMEOUT=3600
VITE_ENABLE_REQUEST_SIGNING=false
VITE_CORS_ALLOWED_ORIGINS=
VITE_ENABLE_ERROR_REPORTING=false
VITE_ERROR_REPORTING_URL=
VITE_RATE_LIMIT_MAX_REQUESTS=100
VITE_RATE_LIMIT_WINDOW_MS=60000
VITE_CSP_ENABLED=false

# Notes:
# - Keep secrets (API keys, anon keys) out of this file. Use your CI/CD secret store for production values.
# - For local dev: create a `.env.local` with VITE_API_URL_LOCAL=/api and run the backend on port 8082; Vite will proxy /api -> http://localhost:8082
# - Copy this file to .env.local / .env.dev / .env.staging / .env.prod as needed and fill in secrets there.
